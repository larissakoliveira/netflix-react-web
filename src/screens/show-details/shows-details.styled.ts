import styled from 'styled-components';
import { Grid } from '@mui/material';

export const Container = styled(Grid)`
    color: #fff;
    text-align: center;

    img {
        width: 100%;
        opacity: 0.8;
    }
`;
